version: "3.8"

services:

  frontend:
    build: ./frontend
    container_name: portfolio-frontend
    ports:
      - "5173:5173"

  backend:
    build: ./backend
    container_name: portfolio-backend
    ports:
      - "30001:30001"
    env_file:
      - ./backend/.env